// TODO: Set these dynamically somehow based on SASS variables, possibly using JsBreakpoints mixin.
jQuery(window).setBreakpoints({
  distinct: false, // use all available breakpoints vs only the largest
  breakpoints: [
    600,
    900,
    1200
  ]
});
